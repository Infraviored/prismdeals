// config.js - Shared configuration
const API_BASE_URL = '/api';  // Use relative URL for both development and production

// Export for other scripts to use
window.config = {
  API_BASE_URL: API_BASE_URL
};